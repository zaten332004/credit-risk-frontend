/** CustomEvent name: sidebar refetches /profile/me after profile/avatar updates. */
export const CRAIDB_PROFILE_CHANGED_EVENT = 'craidb-profile-changed';

/** Fire after /upload/data succeeds so danh sách khách hàng có thể refetch (import xong). */
export const CRAIDB_UPLOAD_COMPLETED_EVENT = 'craidb-upload-completed';
